const boton = getElementById('boton');
const parrafo = getElementById('message');


//function where the post is created
boton.onmouseclicK("mouseclick", textArea);
//it has to creat a h2 element with the input of the user
function textArea(){
    document.createElement("textarea")
}
function create_p(textArea){
    doment.createElement("h2");

}

//the text area is already created you have to create a function tha takes texarea input and log it as h2 to the site.